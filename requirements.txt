# Core
# Install torch + torchvision from the cu124 wheel index to lock CUDA to 12.4:
#   pip install --index-url https://download.pytorch.org/whl/cu124 torch torchvision
# torchvision is required (qwen-vl-utils imports it eagerly at top of module).
# The previous <2.6 pin guarded against torch wheels shipping CUDA 13
# (incompatible with bnb + the RunPod host driver) — irrelevant as long as you
# install from the cu124 index.
torch>=2.5
torchvision>=0.20
transformers>=4.46.0
accelerate>=1.0.0

# QLoRA stack
peft>=0.13.0
trl>=0.12.0
bitsandbytes>=0.44.0

# Data + Qwen-VL helpers
datasets>=3.0.0
qwen-vl-utils>=0.0.8
pillow>=10.0.0

# Eval / misc
tqdm
openai>=1.50.0
